# فقط env_file داکر را حذف می‌کند — mount در deploy/instances/<domain>/docker-compose.yml است
services:
  web:
    env_file: !reset null

  queue:
    env_file: !reset null

  scheduler:
    env_file: !reset null
