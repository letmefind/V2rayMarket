# فقط شبکهٔ دادهٔ مشترک؛ bindهای .env در docker-compose.mount.yml (آخرین فایل merge) اضافه می‌شوند.
networks:
  shared_data:
    external: true
    name: ${SHARED_DATA_NETWORK:-vpnmarket_shared_data}
